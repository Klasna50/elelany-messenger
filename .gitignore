node_modules
dist
dist-ssr
release
*.local
.netlify

# Generated TS/Vite artifacts (must not shadow the .ts sources)
*.tsbuildinfo
vite.config.js
vite.config.d.ts
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# The landing/marketing site lives in its own repo, outside this folder
# (moved to ../elelany-landing). Kept here so a stray landing/ never gets
# committed into the app repo again.
landing/
